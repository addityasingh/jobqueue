describe("Job stack", () => {
  describe("with single concurrency", () => {
    test.todo("create single concurrency stack successfully");
    test.todo("create job stack successfully with default options");
    test.todo("create job stack successfully with provided options");
    test.todo("queue and process single job");
    test.todo("queue and process jobs within max limit");
    test.todo("queue and cancel jobs with timeout");
    test.todo("queue and cancel jobs when stack full");
  });

  describe("with multiple concurrency", () => {
    test.todo("create stack with max concurrency successfully");
    test.todo("queue and process job in stack with multiple concurrency");
  });
});
